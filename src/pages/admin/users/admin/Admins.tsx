import React from "react";
import styles from "../styles/allStyles.module.css";

const Admins = () => {

    return (
        <div className={styles.users}>
            Здесь будут администраторы
        </div>
    );
};

export default Admins;
